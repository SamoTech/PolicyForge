# AL-001 — Enable AppLocker Application Identity Service

**ID:** AL-001  
**Category:** AppLocker / Prerequisites  
**Risk Level:** 🟠 Medium  
**OS:** Windows 10 Enterprise/Education, Windows 11  
**Source:** Microsoft AppLocker Documentation · learn.microsoft.com

---

## Policy Path

```
Computer Configuration
  └─ Windows Settings
       └─ Security Settings
            └─ Application Control Policies
                 └─ AppLocker
                      └─ (Configure rules — requires Application Identity service running)

Services:
  └─ Application Identity (AppIDSvc) → Startup Type: Automatic
```

## Registry

```
Key:   HKLM\SYSTEM\CurrentControlSet\Services\AppIDSvc
Value: Start
Type:  REG_DWORD
Data:  2  (2=Automatic, 3=Manual, 4=Disabled)
```

## PowerShell

```powershell
# Enable and start Application Identity service
Set-Service -Name AppIDSvc -StartupType Automatic
Start-Service -Name AppIDSvc

# Verify
Get-Service AppIDSvc | Select Name, Status, StartType

# Check AppLocker enforcement status
Get-AppLockerPolicy -Effective | Format-List
```

## Intune CSP

```
# Configure via Intune > Endpoint Security > Attack Surface Reduction
# Or deploy via PowerShell script in Intune:
Set-Service -Name AppIDSvc -StartupType Automatic
Start-Service -Name AppIDSvc
```

## Description

The Application Identity service (AppIDSvc) is the prerequisite for AppLocker enforcement. Without this service running, AppLocker rules are defined in policy but **never enforced** — apps run freely regardless of rules. This is a critical configuration gap often missed in AppLocker deployments. The service must be set to Automatic startup so it persists across reboots.

## Impact

- ✅ Prerequisite for all AppLocker enforcement
- ✅ Low resource footprint — minimal performance impact
- ⚠️ AppLocker rules are silently unenforced if this service is not running
- ℹ️ Can be managed via Group Policy Preferences > Services

## Compliance References

- **CIS Benchmark:** Windows 11 v3.0 — Required for AppLocker deployment
- **Microsoft Security Baseline:** Prerequisite documentation

## Test Status

✔ Tested on Windows 11 24H2
✔ Without service: rules defined but unenforced (verified)
