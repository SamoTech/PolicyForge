# FW-001 — Enable Windows Firewall on All Profiles (Domain/Private/Public)

**ID:** FW-001  
**Category:** Windows Firewall / Baseline  
**Risk Level:** 🔴 Critical  
**OS:** Windows 10+, Windows 11, Windows Server 2016+  
**Source:** Microsoft Security Baseline · CIS Benchmark Windows 11 v3.0

---

## Policy Path

```
Computer Configuration
  └─ Windows Settings
       └─ Security Settings
            └─ Windows Defender Firewall with Advanced Security
                 └─ Windows Defender Firewall with Advanced Security — Local Group Policy Object
                      ├─ Domain Profile:  Firewall State: On | Inbound: Block | Outbound: Allow
                      ├─ Private Profile: Firewall State: On | Inbound: Block | Outbound: Allow
                      └─ Public Profile:  Firewall State: On | Inbound: Block | Outbound: Allow
```

## Registry

```
; Domain Profile
HKLM\SOFTWARE\Policies\Microsoft\WindowsFirewall\DomainProfile
  EnableFirewall = 1
  DefaultInboundAction = 1    (1=Block, 0=Allow)
  DefaultOutboundAction = 0   (0=Allow, 1=Block)
  DisableNotifications = 1

; Private Profile
HKLM\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile
  EnableFirewall = 1
  DefaultInboundAction = 1
  DefaultOutboundAction = 0

; Public Profile
HKLM\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile
  EnableFirewall = 1
  DefaultInboundAction = 1
  DefaultOutboundAction = 0
  DisableNotifications = 0    (Allow notifications on public networks)
```

## PowerShell

```powershell
# Enable all three profiles
Set-NetFirewallProfile -Profile Domain,Private,Public -Enabled True

# Set default inbound action to Block
Set-NetFirewallProfile -Profile Domain,Private,Public `
                       -DefaultInboundAction Block `
                       -DefaultOutboundAction Allow

# Verify
Get-NetFirewallProfile | Select Name, Enabled, DefaultInboundAction, DefaultOutboundAction
```

## Description

Ensures Windows Firewall is enabled and actively enforcing rules on all three network profiles. The default inbound-block posture means any inbound connection not explicitly allowed by a rule is dropped. This is the foundation of the Windows Firewall configuration — without it, all subsequent firewall rules are irrelevant.

## Impact

- ✅ All inbound traffic blocked unless explicitly allowed
- ✅ Protects against unsolicited inbound connections across all network types
- ⚠️ May block custom line-of-business applications that need inbound connections
- ℹ️ Public profile is the most critical — laptops on public Wi-Fi rely on this

## Compliance References

- **Microsoft Security Baseline:** Windows 11 24H2 (On/Block/Allow)
- **CIS Benchmark:** Windows 11 v3.0 — 9.1.1, 9.2.1, 9.3.1 (L1)
- **DISA STIG:** WN11-NX-000015
- **NIST 800-41:** Firewall policy fundamentals

## Test Status

✔ Tested on Windows 11 24H2
✔ All three profiles verified enabled via Get-NetFirewallProfile
