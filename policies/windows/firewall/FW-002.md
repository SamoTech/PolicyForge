# FW-002 — Enable Firewall Logging (Dropped Packets + Successful Connections)

**ID:** FW-002  
**Category:** Windows Firewall / Logging  
**Risk Level:** 🟠 Medium  
**OS:** Windows 10+, Windows 11  
**Source:** CIS Benchmark Windows 11 v3.0

---

## Policy Path

```
Computer Configuration
  └─ Windows Settings
       └─ Security Settings
            └─ Windows Defender Firewall with Advanced Security
                 └─ [Each Profile] > Customize Logging
                      ├─ Log dropped packets: Yes
                      ├─ Log successful connections: Yes
                      ├─ Size limit: 16,384 KB
                      └─ Log file path: %SYSTEMROOT%\System32\logfiles\firewall\pfirewall.log
```

## Registry

```
; Example for Domain profile (repeat for Private and Public)
HKLM\SOFTWARE\Policies\Microsoft\WindowsFirewall\DomainProfile\Logging
  LogDroppedPackets  = 1
  LogSuccessfulConnections = 1
  LogFilePath = "%SYSTEMROOT%\System32\logfiles\firewall\pfirewall.log"
  LogFileSize = 16384
```

## PowerShell

```powershell
# Enable firewall logging for all profiles
Set-NetFirewallProfile -Profile Domain,Private,Public `
    -LogFileName "%SYSTEMROOT%\System32\logfiles\firewall\pfirewall.log" `
    -LogMaxSizeKilobytes 16384 `
    -LogBlocked True `
    -LogAllowed True

# Verify
Get-NetFirewallProfile | Select Name, LogFileName, LogBlocked, LogAllowed, LogMaxSizeKilobytes

# Read recent log entries
Get-Content "$env:SYSTEMROOT\System32\logfiles\firewall\pfirewall.log" -Tail 50
```

## Log Format

```
#Version: 1.5
#Fields: date time action protocol src-ip dst-ip src-port dst-port size ...
2026-04-17 01:00:00 DROP TCP 192.168.1.50 10.0.0.5 54321 445 ...
2026-04-17 01:00:01 ALLOW TCP 10.0.0.5 8.8.8.8 49152 443 ...
```

## Description

Enables firewall logging for all three network profiles. Without logging, there is no visibility into what traffic is being blocked or allowed. The 16MB log size ensures sufficient history without consuming excessive disk space. Dropped packet logging is essential for detecting port scans, lateral movement attempts, and misconfigured applications.

## Impact

- ✅ Visibility into blocked and allowed traffic per endpoint
- ✅ Enables detection of lateral movement (SMB/RDP scanning)
- ✅ Essential for incident response and forensics
- ⚠️ Logs are local only — forward to SIEM for enterprise visibility (see Audit Policy batch)
- ℹ️ 16 MB covers ~1M events — increase to 32 MB on high-traffic servers

## Compliance References

- **CIS Benchmark:** Windows 11 v3.0 — 9.1.7, 9.2.7, 9.3.7 (L1)
- **DISA STIG:** WN11-NX-000020
- **NIST 800-92:** Log management fundamentals

## Test Status

✔ Tested on Windows 11 24H2
✔ pfirewall.log populated with DROP/ALLOW entries
