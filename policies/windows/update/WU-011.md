# WU-011 — Disable Windows Update Access on Metered Connections

**ID:** WU-011  
**Category:** Windows Update / Network / Bandwidth  
**Risk Level:** 🟢 Low  
**OS:** Windows 10+, Windows 11  
**Source:** Microsoft Documentation

---

## Policy Path

```
Computer Configuration
  └─ Administrative Templates
       └─ Windows Components
            └─ Windows Update
                 └─ Manage end user experience
                      └─ Allow updates to be downloaded automatically over metered connections
                           └─ Disabled
```

## Registry

```
HKLM\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate
  AllowAutoWindowsUpdateDownloadOverMeteredNetwork = 0
  ; 0 = Do NOT download over metered connection
  ; 1 = Allow download over metered connection
```

## PowerShell

```powershell
# Disable WU over metered connections
Set-ItemProperty -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate" `
    -Name "AllowAutoWindowsUpdateDownloadOverMeteredNetwork" -Value 0

# Check if connection is currently metered
$profile = [Windows.Networking.Connectivity.NetworkInformation,Windows.Networking.Connectivity,ContentType=WindowsRuntime]::GetInternetConnectionProfile()
$profile.GetConnectionCost().NetworkCostType
# Unrestricted = not metered, Variable/Fixed = metered
```

## Description

Prevents Windows Update from automatically downloading updates when the network connection is marked as metered (e.g., cellular data, mobile hotspot, satellite). This is critical for laptops that roam between networks — without this policy, a laptop connecting via a mobile hotspot could download several gigabytes of Windows updates, causing unexpected data charges. Updates will download once the device returns to an unmetered connection.

## Impact

- ✅ Prevents unexpected data charges on metered connections
- ✅ Essential for field devices, remote workers with limited data plans
- ⚠️ Devices on metered connections will delay patch installation
- ℹ️ Security-critical updates (out-of-band) may need manual intervention for metered devices

## Test Status

✔ Tested on Windows 11 24H2 with metered connection simulation
